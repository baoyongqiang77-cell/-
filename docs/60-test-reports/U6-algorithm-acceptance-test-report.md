# U6 一期 14 算法验收测试文档

## 验收范围

- 一期 14 个算法编码固定且完整。
- 模拟算法不得计入 M2 一期最终验收。
- 真实算法验收证据必须包含真实模型、达标指标、冻结验收集 checksum、回放记录、人工抽检记录。
- 任一算法缺少证据时，14 算法整体验收不通过。

## 自动化用例

| 用例 | 结果 |
| --- | --- |
| 注册表包含且仅包含固定 14 个算法编码 | PASS |
| 模拟算法即使指标高也不能计入 M2 | PASS |
| 单个真实算法证据完整且指标达标时可通过规则校验 | PASS |
| 缺少任一算法证据时整体验收失败 | PASS |

## 验收结论

U6 验收规则通过。实际 M2 算法验收当前为 **BLOCKED**：仓库内没有 14 个真实模型、冻结验收集 checksum、离线指标报告、视频回放记录或人工抽检记录，因此不得声明 U6 真实算法验收通过。

已新增交付跟踪表：`docs/40-delivery-tracking/algorithm-delivery-tracker.md`。后续每个算法按该表补齐模型、冻结集、指标、回放、抽检和双硬件证据后，才能把单算法状态从 `PENDING/BLOCKED` 调整为 `PASS`。
